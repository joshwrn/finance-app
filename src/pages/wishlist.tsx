import type { FC } from 'react'

import { PageWrapper } from '@components/PageWrapper'

const Home: FC = () => {
  return <PageWrapper title="Wishlist" categoryType="WISHLIST" />
}

export default Home
